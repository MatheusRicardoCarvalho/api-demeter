version: 0.2

phases:
  pre_build:
    commands:
      - echo Install packages...
      - npm install
  build:
    commands:
      - echo Starting application...
      - npm run start
artifacts:
  base-directory: '.'
  files:
    - '**/*'
